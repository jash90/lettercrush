/**
 * Theme System
 * Central exports for LetterCrush design tokens
 */

export { colors } from './colors';
export type { Colors, ColorKey } from './colors';

export { spacing, borderRadius, fontSize, animation } from './spacing';
export type { Spacing, SpacingKey, BorderRadius, FontSize, Animation } from './spacing';

// Unified theme object for convenience
export { colors as theme } from './colors';
